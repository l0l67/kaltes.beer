# Source code for my Website (coming soon™)
- execute `DB.py` to initialize the sqlite database
- the port and interface ip can be set in `server.sh`
- Post-Timeout for the Guestbook can be set in `main.py`